include asennawordpress

#mysql-rooti
class { 'mysql::server':
	root_password => '',n salasana
}

#wordpress tietokannan käyttäjän tiedot
mysql::db { 'wordpress':
  user     => 'wordpressuser',
  password => 'wordpressuserpassword',
  host     => 'localhost',
  grant    => ['ALL'],
}
