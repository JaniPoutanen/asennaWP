include asennawp

#mysql-rootin salasana
class { 'mysql::server':
	root_password => '',
}

#wordpress tietokannan käyttäjän tiedot
mysql::db { 'wordpress':
  user     => 'wordpressuser',
  password => 'wordpressuserpassword',
  host     => 'localhost',
  grant    => ['ALL'],
}
