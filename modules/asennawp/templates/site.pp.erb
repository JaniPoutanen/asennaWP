include asennawp

class { 'mysql::server':
	root_password => 'wwww.§ponen€.FI7',}

mysql::db { 'wordpress':
  user     => 'wordpressuser',
  password => 'wordpressuserpassword',
  host     => 'localhost',
  grant    => ['ALL'],
}
